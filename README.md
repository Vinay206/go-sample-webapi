# go-sample-webapi

```sh
git clone --no-checkout --filter=blob:none --sparse https://github.com/Vinay206/go-sample-webapi.git
cd go-sample-webapi
git sparse-checkout init --cone
git sparse-checkout set services/orders-api
git checkout
```
